{
  "name": "phishguard",
  "version": "1.0.0",
  "private": true,
  "description": "PhishGuard — cybersecurity URL analysis and phishing education platform",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "preview": "cd frontend && npm run preview",
    "lint": "cd frontend && npm run lint"
  },
  "engines": {
    "node": ">=20"
  }
}
